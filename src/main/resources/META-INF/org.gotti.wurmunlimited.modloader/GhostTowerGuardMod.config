# How close do targets need to be to a tower? default to 20 tiles.
help_respond_range=100


# Names used with mod loader's IdFactory tool. They must mach the template json importer field "id_factory_name".
# Type string.
black_tower_id_name=jdbBlackTower
fort_tower_id_name=jdbFortTower
round_tower_id_name=jdbRoundTower
square_tower_id_name=jdbSquareTower
tower_ghost_id_name=jdbTowerGhost